## 项目主要需求

### 项目概述

构建一个面向高校学生的校园二手书交易平台，学生可发布闲置教材和书籍进行出售或交换，其他同学可按学科、课程名搜索所需教材并联系卖家交易。平台旨在降低学生教材购买成本，促进校园内书籍循环利用。

### 功能需求

1. **用户注册与认证**：学生使用学号和学校邮箱注册，通过邮箱验证码完成身份验证，注册后完善个人信息（姓名、院系、联系方式），支持上传头像
2. **发布闲置书籍**：卖家发布书籍信息（书名、作者、ISBN、原价、出售价格、新旧程度：全新/九成新/七成新/五成新），支持拍照上传最多 5 张实物图片，选择交易方式（面交/邮寄），填写书籍所在校区
3. **书籍搜索与浏览**：买家可按书名、作者、ISBN 搜索，支持按学科分类筛选（理工、文史、经管、艺术等），支持按价格区间和新旧程度筛选，搜索结果支持按价格、发布时间排序
4. **求购信息**：买家可发布求购需求（需要哪本书、期望价格、新旧要求），卖家看到求购信息后可主动联系，求购信息支持按学科分类浏览
5. **交易沟通**：买卖双方通过平台内置消息功能沟通交易细节，消息支持文字和图片，支持标记书籍为"已预约"和"已售出"状态
6. **交易评价**：交易完成后双方互相评价（好评/中评/差评 + 文字评价），用户主页展示累计好评率和历史评价记录，好评率低的用户标记风险提示
7. **书籍收藏与浏览历史**：买家可收藏感兴趣的书籍方便后续查看，系统自动记录用户浏览历史，首页推荐同院系同学发布的书籍

### 技术要求

- 前端：Vue 3 + TypeScript，使用 Vant 4 移动端组件库（适配手机端），Vite 构建工具
- 后端：Node.js + Express 框架，TypeScript 开发
- 数据库：MySQL 8.0，使用 TypeORM 做数据持久化
- 缓存：Redis 用于热门搜索缓存和会话管理
- 文件存储：MinIO 存储书籍图片
- 认证方式：JWT + 邮箱验证码
- 搜索：MySQL 全文索引实现书籍搜索

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：

页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 暴露端口（避免与其他项目冲突）：前端 8011，后端 3011，MySQL 3404，Redis 6411，MinIO 9007/9008
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

